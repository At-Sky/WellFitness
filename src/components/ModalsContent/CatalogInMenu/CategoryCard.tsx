import React from 'react';

interface ICategoryCard {
    name: string
    img: string
}

function CategoryCard({name, img}: ICategoryCard) {
    return (
        <div></div>
    );
}

export default CategoryCard;
